---
description: Force a PDF list/brochure to re-render
---
Call the `esperanza-ops` Tier-2 tool `rebuild_pdf` with `list` = $ARGUMENTS, then poll
`sync_status` / report when the render is live. Renders are async.
